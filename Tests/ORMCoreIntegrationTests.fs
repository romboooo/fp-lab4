namespace ORM.Tests

open System
open Xunit
open ORM.ORMCore

module ORMCoreIntegrationTests =
    
    [<Fact>]
    let ``CRUD operations should handle different scenarios`` () =
        Assert.True(true)
    
    [<Fact>]
    let ``TableContext should be creatable`` () =
        Assert.True(true)